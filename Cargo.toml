# Cargo.toml
[package]
name = "rust-modules"
version = "0.1.0"
edition = "2021"

[lib]
name = "_core"
crate-type = ["cdylib"]
path = "rust/src/lib.rs"

[dependencies]
pyo3 = { version = "0.29.0", features = ["extension-module"] }

